Readme

Create a conda virtual environment to run the plotly dash app. (Requires anaconda)

Install the requirements.txt (copy paste line by line)

Run:
	 python app.py


Notes:

The code has some style on html that needs to be removed and exchange for a css stylesheet file. 

Css stylesheet files need to be saved on assets folder, in the same address as the app.py file. 

Icons and images also need to be stores on assets folder. 
